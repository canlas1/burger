<div class="container">
<div class="row">
  <div class="col-xs-12">
    <h1>burgers!</h1>
  </div>
</div>
<div class="row">
  <div class="col-xs-12">
    <h2>Add a burger</h2>
      <form class="create-update-form" action="/burger/create" method="POST", id= "form1">

      <div class="form-group">
        <label for="ca">burger Name:</label>
        <input type="text" id="ca" name="name">
      </div>

    {{!--   <div class="form-group">
        <label for="slee">devoured?</label><br>
        <input type="radio" name="devoured" value="1">checked devoured!<br>
        <input type="radio" name="devoured" value="0"> Test
      </div> --}}

      <button type="submit">Add burger</button>
      </form>
  </div>
</div>
<div class="row">
  <div class="col-xs-6">
  <h2>burgers that are regurgitated!</h2>

  <ul>
    {{#each burgers}}
      {{#unless this.devoured}}
        <li>
         {{this.burger_name}}

          <form action="/burgers/eat?_method=PUT" method="POST">
            <input type="hidden" name="burger_id" value={{this.id}}>
            <button type="submit">BURGER TIME!</button>
          </form>

          {{!-- {{> burgers/delete-burger}} --}}
        </li>
      {{/unless}}
    {{/each}}
  </ul>
  </div>
  <div class="col-xs-6">
    <h2>burgers that are devoured!</h2>
    <ul>
      {{#each burgers}}
        {{#if this.devoured}}
          <li>
            {{this.burger_name}}

            <form action="/burgers/regurgitate?_method=PUT" method="POST">
              <input type="hidden" name="burger_id" value={{this.id}} id= "input_devoured">
              <button type="submit">BURGER TIME!</button>
            </form>

            {{!-- {{> burgers/delete-burger}} --}}
          </li>
        {{/if}}
      {{/each}}
    </ul>
    </div>
</div>
<script type="text/javascript">
  
</script>








</div>